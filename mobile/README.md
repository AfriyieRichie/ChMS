# Mobile (Phase 5 placeholder)

React Native + Expo (SDK 51+) member-facing app. Implementation begins in Phase 5.

Planned:
- `expo-router` for navigation
- `expo-secure-store` for token storage
- Shared Zod schemas and API types with the `web/` package
- Push notifications, offline support
